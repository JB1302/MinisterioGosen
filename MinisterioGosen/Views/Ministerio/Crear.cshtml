@model MinisterioGosen.Models.MinisterioModel

@{
    ViewData["Title"] = "Nuevo ministerio";
}

<div class="container py-4">

    <div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-3 mb-4">
        <div>
            <h2 class="fw-bold mb-1">
                <i class="bi bi-plus-circle-fill me-2 text-primary"></i>
                Nuevo ministerio
            </h2>
            <p class="text-muted mb-0">
                Registre un nuevo ministerio o grupo de apoyo.
            </p>
        </div>

        <a asp-controller="Ministerio"
           asp-action="Index"
           class="btn btn-outline-secondary rounded-pill px-4">
            <i class="bi bi-arrow-left me-1"></i>
            Volver
        </a>
    </div>

    <div class="card border-0 shadow-sm rounded-4">
        <div class="card-body p-4">

            @if (ViewBag.Mensaje != null)
            {
                <div class="alert alert-warning d-flex align-items-center gap-2 rounded-3">
                    <i class="bi bi-exclamation-triangle-fill"></i>
                    <span>@ViewBag.Mensaje</span>
                </div>
            }

            <div id="mensajeValidacion" class="alert alert-danger d-none">
                <i class="bi bi-exclamation-triangle-fill me-1"></i>
                <span id="textoValidacion"></span>
            </div>

            <form asp-action="Crear"
                  asp-controller="Ministerio"
                  method="post"
                  id="formMinisterio"
                  novalidate>

                <div class="row g-4">

                    <div class="col-md-12">
                        <label class="form-label fw-semibold">
                            <i class="bi bi-people-fill me-1 text-muted"></i>
                            Nombre del ministerio
                        </label>

                        <input asp-for="Descripcion_Ministerio"
                               class="form-control rounded-3"
                               placeholder="Ingrese el nombre del ministerio" />

                        <div class="invalid-feedback">
                            Debe ingresar el nombre del ministerio.
                        </div>
                    </div>

                    <div class="col-md-12">
                        <label class="form-label fw-semibold">
                            <i class="bi bi-chat-left-text-fill me-1 text-muted"></i>
                            Observaciones
                        </label>

                        <textarea asp-for="Observaciones_Ministerio"
                                  class="form-control rounded-3"
                                  rows="4"
                                  placeholder="Ingrese observaciones, si aplica"></textarea>

                        <div class="invalid-feedback">
                            Las observaciones no pueden superar 200 caracteres.
                        </div>
                    </div>

                </div>

                <hr class="my-4" />

                <div class="d-flex justify-content-end gap-2">
                    <a asp-controller="Ministerio"
                       asp-action="Index"
                       class="btn btn-outline-secondary rounded-pill px-4">
                        Cancelar
                    </a>

                    <button type="submit"
                            class="btn btn-primary rounded-pill px-4">
                        <i class="bi bi-save-fill me-1"></i>
                        Guardar ministerio
                    </button>
                </div>

            </form>

        </div>
    </div>
</div>

@section Scripts {
    <script src="~/js/ministerio.js" asp-append-version="true"></script>
}